name: Install native ONNX wheel
description: Installs the full published onnx-light wheel for the active Python interpreter.
runs:
  using: composite
  steps:
    - shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
      run: python .github/scripts/install_onnx_light.py
